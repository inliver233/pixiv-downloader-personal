# PixivUtil2（本修改版）项目功能介绍

> 本文根据仓库源码与现有说明整理，面向“快速了解项目功能与结构”的用途。

## 1. 项目概览
PixivUtil2 是一个面向 Pixiv 的命令行下载器与数据库管理工具，本修改版在原有 CLI 基础上增加了简易中文 Web UI（`web_ui.py`）。项目支持 Pixiv 作品、收藏、排行、新作、小说、系列、未公开链接（unlisted），并扩展支持 FANBOX 与 Pixiv Sketch。下载过程带有完整的数据库记录、去重校验、文件命名规则与后处理能力。

## 2. 主要入口与运行方式
- CLI 主入口：`PixivUtil2.py`（交互式菜单 + 命令行参数）
- 数据库管理入口：`PixivDBManager.py`
- Web 可视化入口：`web_ui.py`（浏览器操作，面向“关注同步/URL 索引/快速浏览”）

## 3. 功能总览（按业务维度）

### 3.1 Pixiv 作品下载与检索
支持多种来源与筛选方式，核心逻辑集中于 `handler/` 下的各类处理器：
- 按用户（member_id）下载
- 按作品（image_id）下载
- 按标签（tags）下载
- 按标题/简介（Title/Caption）下载
- 按“标签 + 作者”组合下载
- 按关注画师作品下载（含私密）
- 按收藏作品下载（含私密）
- 下载“收藏的新作”
- 按组（Group ID）下载
- 按漫画系列（Manga Series ID）下载
- 按未公开链接（Unlisted image_id）下载
- Pixiv 排行榜下载（含 R-18）
- Pixiv 新作流（New Illusts）

### 3.2 Pixiv 小说与系列
`handler/PixivNovelHandler.py` 支持：
- 小说单篇下载（Novel ID）
- 小说系列下载（Novel Series ID）
- 自动保存正文为本地文件，并写入数据库记录

### 3.3 FANBOX 支持
`handler/PixivFanboxHandler.py` 支持：
- 从支持列表下载（支持的创作者）
- 按创作者/artist id 下载
- 按 post id 下载
- 下载关注列表
- 下载自定义列表
- 可选生成 FANBOX 帖子 HTML（`template.html` / `novel_template.html`）
- 支持受限内容的封面下载（可配置）

### 3.4 Pixiv Sketch 支持
`handler/PixivSketchHandler.py` 支持：
- 按 Sketch 创作者下载
- 按 Sketch post id 下载
- 记录下载历史以便增量

### 3.5 批处理与列表驱动下载
`handler/PixivBatchHandler.py` / `handler/PixivListHandler.py` 支持：
- `batch_job.json` 批量任务（可配置下载目标与参数）
- `list.txt` / `tags.txt` 驱动批量下载
- `ignore_list.txt` / `blacklist_*` 文件驱动跳过策略

### 3.6 关注画师「URL 索引」模式（本修改版新增）
为“**不全量下载**、只保存作品图片 URL + 预览图抽样”的工作流新增了专用模式：
- 同步你的关注列表：新增关注会自动建档；取消关注会自动清理（URL + 预览图）
- 为每个画师保存其所有作品的图片 URL（含多 P）
- 每个画师最多下载少量预览图（默认 3 张，存放于 `rootDirectory/_preview/<member_id>/`）
- CLI 菜单新增选项：`20. Sync followed artists (URL index + previews)`
- 核心实现：`handler/PixivFollowIndexHandler.py`
- DB 增量表：`pixiv_follow_member / pixiv_follow_image / pixiv_follow_image_url`

## 4. 文件处理与后处理能力

### 4.1 文件去重与校验
下载过程会结合数据库与本地文件进行去重与校验：
- DB 记录存在时默认跳过
- 可配置校验文件大小/修改时间
- 支持“总是检查大小”模式

### 4.2 Ugoira 动图处理
支持将 Pixiv 动图（ugoira）导出为多种格式：
- `.ugoira`、`.mkv`、`.webm`、`.webp`、`.gif`、`.apng`、`.avif`
- 依赖 FFmpeg；参数可通过配置自定义

### 4.3 归档模式（Archive）
可将作品下载为 ZIP 归档而非目录结构，适合归档保存与迁移：
- 自动生成 ZIP
- 自动写入数据库路径
- 对命名格式有路径分隔要求

### 4.4 元数据与信息输出
可选输出：
- 作品 JSON / 原始 JSON
- 作品信息文本
- XMP 元数据（用于管理软件）
- 作品网址写入描述

## 5. 数据库管理（本地 SQLite）
`PixivDBManager.py` 提供完整的数据库维护能力：
- 成员、作品、标签、系列、FANBOX、Sketch、小说数据的记录与维护
- 导出本地下载记录
- 删除成员/作品记录（含级联）
- 清理数据库（文件不存在则清理记录）
- 黑名单记录与历史查询

> 数据表覆盖：member、image、manga、tag、series、fanbox post、sketch post、novel。

## 6. 配置与可定制项（config.ini）
核心配置集中在 `common/PixivConfig.py` 与 `readme.md` 中，常用配置包括：
- 登录/认证：账号密码、cookie、refresh_token
- 网络：代理、超时、重试、下载延迟、镜像源
- 下载策略：页数、R-18 过滤、增量下载、黑名单
- 文件命名：多种占位符组合（作者、标题、标签、日期等）
- 标签翻译：启用翻译与语言选择
- 写入 JSON/XMP/Info 及 HTML 的开关

## 7. Web UI（简易中文交互层）
`web_ui.py` 提供一个轻量级的浏览器界面（重构增强）：
- 配置管理（账号/cookie/镜像/延迟等）
- 一键执行「关注同步（URL 索引 + 预览图）」并查看实时日志
- 画师列表浏览（头像/ID/token/作品数/URL 数/同步时间）
- 进入画师详情页查看：预览图、作品列表、单作品 URL 查看与复制、URL 导出

## 8. 目录结构与模块职责
- `PixivUtil2.py`：CLI 主入口、菜单、参数解析、流程调度
- `PixivDBManager.py`：SQLite DB 管理
- `handler/`：下载与流程编排（按来源拆分）
  - `PixivImageHandler.py`：单图/多图处理、归档、ugoira
  - `PixivArtistHandler.py`：作者相关处理
  - `PixivBookmarkHandler.py`：收藏/关注相关处理
  - `PixivTagsHandler.py`：标签检索下载
  - `PixivRankingHandler.py`：排行与新作
  - `PixivFanboxHandler.py`：FANBOX 内容
  - `PixivSketchHandler.py`：Sketch 内容
  - `PixivBatchHandler.py`：批量任务
  - `PixivListHandler.py`：list.txt 下载
  - `PixivFollowIndexHandler.py`：关注同步 + URL 索引 + 预览图抽样（本修改版新增）
- `common/`：配置、浏览器/登录、通用工具、常量、异常
- `model/`：数据模型（image/artist/tag/series/fanbox/sketch/novel）
- `web_ui.py`：Web UI

## 9. 典型流程（概念级）
1. 读取配置与认证信息  
2. 根据模式获取 Pixiv/FANBOX/Sketch 列表  
3. 逐条下载并进行本地/数据库去重判断  
4. 写入文件、元数据、DB 记录  
5. 可选进行 ugoira 处理与后处理命令

## 10. 适用场景示例
- 画师作品归档（含历史与增量）
- 标签批量检索下载
- FANBOX 支持内容本地化备份
- 结合数据库做增量与去重管理
- 通过 Web UI 进行“低门槛操作”

---
如需更深入的“配置参数说明”或“二次开发指南”，可以在此文档基础上继续扩展（例如加入 config.ini 参数对照表、关键类调用关系图、或常见任务脚本）。
